# 06: Contact, footer & latency easter-egg

**What to build:** Dark contact form + toast restyle, footer with real `served in Xms` timing, "dependencies" trust strip (GitHub / LeetCode / IIT). Full page reads top-to-bottom as one design.

**Blocked by:** 01: Motion foundation & dark design system

**Status:** ready-for-agent

- [ ] Form fields restyled dark with mono labels; submit button cyan accent
- [ ] Toast restyled to match dark system; existing success/error behavior unchanged
- [ ] Footer shows real client-side measured render time (`served in Xms`)
- [ ] Trust strip present under hero or footer
- [ ] POST /api/contact flow untouched and working
